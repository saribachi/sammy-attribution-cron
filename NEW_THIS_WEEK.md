# New this week (included in the next Monday sweep, then cleared via commit)
#
# Empty = nothing shipped since the last sweep, and the sweep omits the
# section entirely. Add a bullet per ship; clear this file after it posts.
- **quotes_since_purchase / estimate_count_at_purchase** (new): post-purchase activation signal. estimate_count is absolute, so a HubSpot filter cannot ask "three more since they paid". The baseline is captured once at conversion and the difference maintained hourly. Backfilled across 228 customers 14 Sep. Drives the First Six Weeks flow.
